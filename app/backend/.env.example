# Copy this to .env for local development
NODE_ENV=development
PORT=3000
JWT_SECRET=change-me-to-a-random-32-char-string
SQLITE_PATH=./pmai.db

# Optional: only needed if you add real AI generation later
# ANTHROPIC_API_KEY=
